<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Status</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa; /* Light background color */
            height: 100vh; /* Full height */
        }
        .card {
            margin-top: auto; /* Centering card vertically */
            margin-bottom: auto; /* Centering card vertically */
            max-width: 400px; /* Card max width */
        }
    </style>
</head>
<body>

    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="card shadow">
            <div class="card-body text-center">
                <% if (token) { %>
                    <h1 class="card-title">Signup Successful!</h1>
                    <p class="card-text">Your token: <strong><%= token %></strong></p>
                    <p class="card-text">You will be redirected to the login page in 10 seconds...</p>
                <% } else if (error) { %>
                    <h1 class="card-title text-danger">Error!</h1>
                    <p class="card-text fs-3">Error: <%= error.message || error %></p>
                    <p class="card-text">You will be redirected to the login page in 10 seconds...</p>
                <% } %>

                <a href="/login" class="btn btn-primary mb-2">Click here if not redirected automatically</a>
                <a href="/signup" class="btn btn-primary">Click here to create New User</a>
                
            </div>
        </div>
    </div>

    <script>
        // Automatic redirect after 10 seconds
        setTimeout(() => {
            window.location.href = '/login';
        }, 8000);
    </script>
</body>
</html>
